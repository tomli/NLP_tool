<script>
  $(function () {
    function empty(str) {
      return str == null || str == '';
    }


    $('#loginForm').submit(function (event) {
/*      var username = $('#usernameInput').val();
      if (empty(username)) {
        alert('用户名不能为空');
        return false;
      }*/
      var username = $('#mobileInput').val();
      if (empty(username)) {
        alert('手机号不能为空');
        return false;
      }
      var password = $('#passwordInput').val();
      if (empty(password)) {
        alert('密码不能为空');
        return false;
      }
      return true;
    });
  });
</script>


 <!-- <div class="alert alert-success" role="alert">
    <strong><%=info%></strong>
  </div>-->


<div class="row">
  <div class="col-xs-1 col-lg-3">
  </div>
  <div class="col-xs-10 col-lg-6">
  <div class="panel panel-default well">
    <div class="panel-body form-group">
      <form id="loginForm" class="form-horizontal" action="/login" method="POST">
        <div class="form-group">
          <label for="mobileInput" class="col-sm-2 control-label">手机号</label>
          <div class="col-sm-10">
            <input  id="mobileInput" name="mobile" class="form-control" placeholder="手机号" value="" type="number"/>
          </div>
        </div>
        <div class="form-group">
          <label for="passwordInput" class="col-sm-2 control-label">密码</label>
          <div class="col-sm-10">
            <input id="passwordInput" name="password" class="form-control" placeholder="密码"  type="password">
          </div>
        </div>
        <!--
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <div class="checkbox">
              <label>
                <input type="checkbox"> Remember me
              </label>
            </div>
          </div>
        </div>-->
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-5">
            <button  class="btn btn-primary btn-block"  type="submit" class="btn btn-default">登录</button>
          </div>
          <div class="col-sm-5">
          </div>
        </div>
      </form>

    </div>
  </div>
  </div>
  <div class="col-xs-1 col-lg-3">
  </div>
</div>